# Copy this file to host/.env (same directory you run Python from) and fill in values.
# API keys: https://api-v3.mbta.com/register

# Required for host scripts (test_api.py, tracker.py)
MBTA_API_KEY=

# Optional: serial device for tracker.py (host/config.py has a default if unset)
SERIAL_PORT=
